import 'package:flutter/material.dart';
import 'package:vit_helper/ui/splashScreen.dart';

void main() {
  runApp(
    MaterialApp(
      title: 'VIT Helper',
      home: SplashScreen(),
      debugShowCheckedModeBanner: false,
    )
  );
}